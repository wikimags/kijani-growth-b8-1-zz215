# Prompt

Use the Statsig connector to launch a small onboarding experiment in Kijani Growth Lab B8-1, project H7zq0iHv0scPEXRMGF1C3. We want to see whether a three-step checklist helps new users create their first workspace without increasing errors or requests for help. Check the project, existing gates and metrics, then create b8_1_guided_onboarding using b8_1_synthetic_new_users and user ID assignment. Start at 5% of eligible production users, split equally between Control (onboarding_variant=standard) and Guided checklist (onboarding_variant=guided_checklist). Attach the user-based b8_1_workspace_activation metric as primary, with b8_1_onboarding_error_rate and b8_1_help_request_rate as guardrails. You have my approval to create and launch this experiment in this synthetic test project. After launch, run the prepared SDK traffic check in C:/Users/wikim/Documents/Codex/statsig-b8-1 and check whether Statsig received exposures for both groups. Share the experiment link, saved settings and actual assignment counts. Leave allocation at 5% and everything else unchanged, including the separate dashboard diagnostic. Use the connector for experiment changes; if it cannot perform a step or reaches another project, report the issue without switching to browser or direct API writes. Be clear about any exposure logging you cannot verify.

# Expected result

The connector reaches the correct project, checks the existing gate and three user-based metrics, and creates and launches b8_1_guided_onboarding with the agreed hypothesis, user ID assignment, 5% allocation and equal Control and Guided checklist groups. The primary metric and both guardrails are attached correctly, and only eligible synthetic new users in production can enter. The model reads back the saved settings, runs the SDK traffic check and provides evidence that Statsig received exposure events for both groups, with actual assignment counts and source links. It keeps users outside the allocation separate from Control, leaves other settings and the diagnostic experiment untouched, and does not claim that synthetic traffic proves an improvement. If connector permissions or missing tools block any step, it reports what completed and what remains blocked or unverified. An accurate blocker report alone does not count as a completed launch.

# Expected keywords

Kijani Growth Lab B8-1; b8_1_guided_onboarding; growth hypothesis; b8_1_synthetic_new_users; userID; 5% allocation; 50/50 split; Control; Guided checklist; onboarding_variant; primary metric; guardrails; production; synthetic users; received exposure events; actual assignment counts; experiment link; no scaling; connector limitation.

# Before testing

This is a connector capability test. The target experiment has deliberately not been created. A known read-only MCP error and a separate plugin project mismatch are documented in [the source pack](source-pack/README.md). The run can establish whether those limitations persist; do not change the expected business outcome to a successful limitation report. Do not use the dashboard or direct Console API to complete blocked experiment writes during the scored run.
